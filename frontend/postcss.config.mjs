// Local PostCSS config to avoid inheriting a parent Tailwind setup.
// Keep it minimal to satisfy Vite's CSS pipeline.

export default {
  plugins: [
    // Add PostCSS plugins here if needed, e.g. autoprefixer
  ],
};
