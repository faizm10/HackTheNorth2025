{
  "name": "backend",
  "version": "1.0.0",
  "description": "CLI-only backend for text processing with Gemini",
  "main": "src/cli.ts",
  "scripts": {
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  }
}